body {
  margin: 0;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  background: #f0f0f0;
}

/* Use vertical writing mode to display text top-to-bottom */
.vertical-text {
  writing-mode: vertical-lr;      /* This makes the text flow vertically */
  font-size: 3rem;
  font-family: Arial, sans-serif;
  overflow: hidden;               /* Hide letters when they move out */
}

/* Style for each letter */
.letter {
  display: inline-block;
  opacity: 1;
  animation: disappear 2s forwards;
}

/* Keyframes for the disappear animation */
@keyframes disappear {
  0% {
    opacity: 1;
    transform: translateY(0);
  }
  100% {
    opacity: 0;
    transform: translateY(-100px); /* Moves the letter upward off the container */
  }
}